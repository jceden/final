<p id="notice"><%= notice %></p>
<center><table>
<%= image_tag root_url + "/images/" + @image.filename, alt: @image.filename %><br>
<%=button_to "Cute", image_cute_vote_path(@image), :method => :post%>
Total
<%=button_to "Not"%>
</table></center>

<%if @image.posts.length == 1 %>
	No comments on this image yet, be the first!
	<%= render 'posts/form'%>
<%else%>
<table>
	<%@image.posts.each do |post|%>
	<%if !post.new_record? %>
<tr>
		<%=post.comment%><br>
	Posted by: <%=post.user_id%>
<br>
	Posted on: <%=post.created_at%>
<br>
	<%end%>
	<br>
	<%end%>
</table>
	<%= render 'posts/form'%>
<%end%>
<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
